let pool={feePerGroup:20,golfersPerGroup:4,missedCutPenalty:10,withdrawnPenalty:15,groups:[]};let live={players:[],provider:"loading"};const $=id=>document.getElementById(id);
function norm(v){return String(v||"").trim().replace(/\s+/g," ")}function key(v){return norm(v).toLowerCase().replace(/[.'’]/g,"")}function money(n){return new Intl.NumberFormat(undefined,{style:"currency",currency:"USD",maximumFractionDigits:0}).format(Number(n||0))}function scoreText(n){const v=Number(n||0);return v<0?String(v):v>0?"+"+v:"E"}function scoreClass(n){const v=Number(n||0);return v<0?"score-under":v>0?"score-over":"score-even"}function html(v){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}function show(msg,err=false){$("message").textContent=msg;$("message").className=err?"message error":"message"}
async function parseResponse(res){const text=await res.text();let data;try{data=JSON.parse(text)}catch{data={error:text||res.statusText}}if(!res.ok)throw new Error(data.error+(data.detail?` Detail: ${data.detail}`:"")+(data.note?` ${data.note}`:""));return data}
async function apiGet(url){return parseResponse(await fetch(url,{cache:"no-store"}))}async function apiPost(url,data){const headers={"content-type":"application/json"};if($("adminPin").value)headers["x-admin-pin"]=$("adminPin").value;return parseResponse(await fetch(url,{method:"POST",headers,body:JSON.stringify(data)}))}
async function loadPool(){pool=await apiGet("/api/pool");$("feePerGroup").value=pool.feePerGroup;$("missedCutPenalty").value=pool.missedCutPenalty;$("withdrawnPenalty").value=pool.withdrawnPenalty}async function loadLive(){try{live=await apiGet("/api/live-leaderboard");$("feedStatus").textContent=`${live.provider||"demo"} feed`;$("lastUpdated").textContent=live.updatedAt?new Date(live.updatedAt).toLocaleString():"Updated now"}catch(e){$("feedStatus").textContent="Feed error";$("lastUpdated").textContent=e.message}}
function gmap(){const m=new Map();(live.players||[]).forEach(p=>m.set(key(p.name),p));return m}function eff(name){const p=gmap().get(key(name));if(!p)return{name,total:0,status:"Not Found",thru:"—"};let total=Number(p.total||0);if(p.status==="Missed Cut")total+=Number(pool.missedCutPenalty||10);if(p.status==="Withdrawn")total+=Number(pool.withdrawnPenalty||15);return{...p,total}}function rows(){return(pool.groups||[]).map((g,i)=>{const golfers=(g.golfers||[]).map(eff);const total=golfers.reduce((s,p)=>s+Number(p.total||0),0);const risk=golfers.filter(p=>["Missed Cut","Withdrawn","Not Found"].includes(p.status)).length;return{...g,i,golfers,total,risk}}).sort((a,b)=>a.total-b.total||a.entrant.localeCompare(b.entrant))}
function renderDash(r){const l=r[0];$("leaderName").textContent=l?`${l.entrant} - ${l.label}`:"—";$("leaderScore").textContent=l?scoreText(l.total):"—";$("groupCount").textContent=(pool.groups||[]).length;$("prizePool").textContent=money((pool.groups||[]).length*Number(pool.feePerGroup||20))}
function renderBoard(){const r=rows();renderDash(r);$("leaderboardBody").innerHTML=r.map((g,i)=>`<tr class="${i===0?"leader":g.risk?"risk":""}"><td><strong>${i+1}</strong></td><td><strong>${html(g.entrant)}</strong><br><span class="muted">${html(g.label||"Group "+(g.i+1))}</span></td><td class="${scoreClass(g.total)}">${scoreText(g.total)}</td><td>${g.golfers.map(p=>`<div>${html(p.name)} <span class="${scoreClass(p.total)}">${scoreText(p.total)}</span> <span class="muted">${html(p.status)} • thru ${html(p.thru)}</span></div>`).join("")}</td><td>${g.risk}</td></tr>`).join("")||`<tr><td colspan="5" class="muted">No groups entered yet.</td></tr>`}
function renderGolfers(){$("golferBody").innerHTML=(live.players||[]).map(p=>`<tr><td>${html(p.position||"—")}</td><td><strong>${html(p.name)}</strong></td><td class="${scoreClass(p.total)}">${scoreText(p.total)}</td><td>${html(p.thru||"—")}</td><td><span class="pill">${html(p.status||"Active")}</span></td></tr>`).join("")||`<tr><td colspan="5" class="muted">No scores loaded.</td></tr>`}
function renderGroups(){$("groupsList").innerHTML=(pool.groups||[]).map((g,i)=>`<div class="group-card"><strong>${html(g.entrant)} - ${html(g.label||"Group "+(i+1))}</strong><button class="danger" onclick="removeGroup(${i})">Remove</button><ul>${(g.golfers||[]).map(x=>`<li>${html(x)}</li>`).join("")}</ul></div>`).join("")||`<p class="muted">No groups saved.</p>`}function render(){renderBoard();renderGolfers();renderGroups()}
function addGroup(){const entrant=norm($("entrantName").value),golfers=$("golfers").value.split(/\n|,/).map(norm).filter(Boolean);if(!entrant)return show("Enter the entrant name.",true);if(golfers.length!==4)return show("Each paid group must have exactly 4 golfers.",true);const label=norm($("groupLabel").value)||`Group ${(pool.groups||[]).filter(g=>g.entrant===entrant).length+1}`;pool.groups=pool.groups||[];pool.groups.push({id:String(Date.now()),entrant,label,golfers});$("entrantName").value="";$("groupLabel").value="";$("golfers").value="";show("Group added. Click Save Pool to publish it.");render()}window.removeGroup=function(i){if(!confirm("Remove this group?"))return;pool.groups.splice(i,1);show("Group removed. Click Save Pool to publish it.");render()}
async function savePool(){pool.feePerGroup=Number($("feePerGroup").value||20);pool.missedCutPenalty=Number($("missedCutPenalty").value||10);pool.withdrawnPenalty=Number($("withdrawnPenalty").value||15);pool.golfersPerGroup=4;try{pool=await apiPost("/api/pool",pool);show("Pool saved successfully.");render()}catch(e){show(e.message,true)}}function clearGroups(){if(!confirm("Clear groups on screen? Click Save Pool afterward."))return;pool.groups=[];render()}async function refresh(){await Promise.allSettled([loadPool(),loadLive()]);render()}
$("addGroup").onclick=addGroup;$("savePool").onclick=savePool;$("clearGroups").onclick=clearGroups;$("refreshNow").onclick=async()=>{await loadLive();render()};refresh().then(()=>setInterval(async()=>{await loadLive();render()},60000));
